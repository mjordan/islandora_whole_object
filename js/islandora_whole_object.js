(function (Drupal, $) {
  "use strict";
  var islandoraObjectNid = drupalSettings.islandora_whole_object.nid;
  // console.log('Node id is ' + islandoraObjectNid);
  // This is where we would retrieve the JSON-LD for node/1 and call jsonld-vis.
  // See https://github.com/mjordan/islandora_whole_object/issues/1.
}) (Drupal, jQuery);

