{#
/**
 * @file
 * Template that wraps content in HTML <pre><code> tags.
 *
 * Available variables:
 * - content: The content to wrap.
 *
 * @ingroup themeable
 */
#}
<div class="islandora_whole_object_content"><pre><code>{{ content }}</code></pre></div>
